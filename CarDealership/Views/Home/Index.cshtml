@* @using CarDealership.Models;

<h1>Car Inventory</h1>
<ul>

@if (Model.Count == 0)
{
  <p>You have no cars in your inventory!</p>
}
@if(Model.Count != 0)
{
  @foreach (Cars car in Model)
  {
    <li>@Model.makeModel, @Model.color, @Model.mileage, @Model.price</li>
  }
}

</ul>
<a href="/cars/new">Add a New Car</a> *@

@using CarDealership.Models;

<h1>Car Inventory</h1>
<ul>

@if (Model.Count == 0)
{
  <p>You have no cars in your inventory!</p>
}
@if(Model.Count != 0)
{
  @foreach (Cars car in Model)
  {
    <li>@Model.makeModel</li>
  }
}

</ul>
<a href="/cars/new">Add a New Car</a>
