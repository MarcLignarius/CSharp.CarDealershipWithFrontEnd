@using CarDealership.Models;

<h1>Car Inventory</h1>
<ul>

@if (Model.Count == 0)
{
  <p>You have no cars in your inventory!</p>
}
@if(Model.Count != 0)
{
  @foreach (Cars car in Model)
  {
    <li>@car.makeModel, @car.color, @car.mileage, @car.price</li>;
  }
}

</ul>
<a href="/cars/new">Add a New Car</a>
